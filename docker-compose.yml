services:
  app:
    build:
      context: ./docker/php
      dockerfile: Dockerfile
      args:
        - PHP_VERSION=8.3
    container_name: reports_app
    restart: unless-stopped
    working_dir: /var/www/html
    volumes:
      - ./src:/var/www/html:delegated
      - ./docker/php/php.ini:/usr/local/etc/php/conf.d/custom.ini

    environment:
      - DB_CONNECTION=pgsql
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_DATABASE=${DB_DATABASE:-cariai_test}
      - DB_USERNAME=${DB_USERNAME:-reports_user}
      - DB_PASSWORD=${DB_PASSWORD:-secret}

    depends_on:
      postgres:
        condition: service_healthy

    networks:
      - reports_network

  nginx:
    build:
      context: ./docker/nginx
      dockerfile: Dockerfile
    container_name: reports_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./src:/var/www/html:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - reports_ssl:/etc/nginx/ssl

    depends_on:
      - app
    networks:
      - reports_network

  postgres:
    image: postgres:16-alpine
    container_name: reports_postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${DB_DATABASE:-cariai_test}
      - POSTGRES_USER=${DB_USERNAME:-reports_user}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-secret}
      - PGDATA=/var/lib/postgresql/data/pgdata
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    ports:
      - "5432:5432"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${DB_USERNAME:-reports_user} -d ${DB_DATABASE:-cariai_test}",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - reports_network

networks:
  reports_network:
    driver: bridge

volumes:
  postgres-data:
  reports_ssl:

